One-line: closes every page with the centred lockup, a short link row and one mono legal line.

```jsx
<SiteFooter items={[{label:'Discord',href:'#'},{label:'Vote',href:'#'}]} logoSrc="assets/elorin-logo.png" />
```

- The live site's footer was not visible in the supplied screenshot - this composition is inferred from the header and hero. Confirm before shipping.

One-line: the progress primitive - gold gradient fill on a sunken navy track, with the count in uppercase mono.

```jsx
<ProgressBar value={12} total={40} />
<ProgressBar value={3} total={7} thin showMeta={false} />
```

- Always render a count somewhere: the bar alone is not an accessible signal.

One-line: embeds a guide video at 16:9 inside the standard bordered panel.

```jsx
<VideoEmbed youtubeId="dQw4w9WgXcQ" caption="Nex duo, full run" />
```

- Uses youtube-nocookie and lazy loading. Without an id it renders the pending placeholder.

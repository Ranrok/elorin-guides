One-line: the 4x7 inventory layout for a recommended setup.

```jsx
<InventoryGrid label="Inventory" items={[{ name: 'Saradomin brew', qty: 4 }, { name: 'Super restore', qty: 2 }]} />
```

- Always 28 cells - don't pad the array with nulls to fill it.
